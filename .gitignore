node_modules/
dist/
.env
server/data/
*.log
.DS_Store
release/

# c8 coverage-отчёты (артефакт CI, не исходники)
coverage/

# SBOM — артефакт CI (Z-06), не исходники
sbom.json

# extra/whisper наполняется скриптом (тяжёлые бинарники не в git)
extra/whisper/*
!extra/whisper/README.md
